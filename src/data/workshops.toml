# Workshop display order. IDs match filenames in workshops/
# Each workshop is defined in its own file: workshops/<id>.toml
#
# Instructor models:
#   speakers = ["speaker-id"]   → references a conference speaker from speakers/
#   [instructor]                → inline instructor for external/community leads
order = [
  "software-design",
  "practical-ai",
  "tinygo",
  "tinygo-keeb",
]
